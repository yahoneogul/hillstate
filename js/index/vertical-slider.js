"use strict";
{
  const sliderList = document.querySelector(".review .slider-list");

  const firstItem = sliderList.firstChild;
  console.log(firstItem);
  const lastItem = sliderList.lastChild;
  //const clonedFirstItem = firstItem.clonedNode(true);
  //const clonedLastItem = lastItem.clonedNode(true);

  //sliderList.append(clonedFirstItem);
  //sliderList.perpend(clonedLastItem);
}
